#!/bin/bash
tmux new-session -d -s dummy
tmux new-session -d -s one /bin/cat
tmux new-session -d -s two /bin/cat
tmux new-session -d -s three
tmux new-session -d -s four
exec tmux new-session -s five
